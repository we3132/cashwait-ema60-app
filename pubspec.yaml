name: cashwait_ema60_min
description: Minimal one-screen trading signal dashboard for '현금대기 EMA60 (U2/D5, B0, C0)'.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  csv: ^6.0.0
  intl: ^0.19.0
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
